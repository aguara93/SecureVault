using Microsoft.AspNetCore.SignalR;

namespace SecureVault.API.Hubs
{
    public class SensorHub : Hub
    {
        // doesn't need to implement any methods for now,
        // just broadcasting messages to connected clients
    }
}
